<!DOCTYPE html>
<html lang="en">
    <head>
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0, minimum-scale=1.0, maximum-scale=1.0, user-scalable=no">
        <title>
            {% block title %}Après-Ski Closing Party
            {% endblock %}
        </title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        <div id="application" class="io-application" {{ stimulus_controller('app') }}>
            <header>
                <turbo-frame id="header" src="{{ path('app_default_header', { isHome: app.request.attributes.get('_route') == 'app_default_index' ? '1' : '0' }) }}"></turbo-frame>
            </header>
            <main>
                <turbo-frame id="content" data-turbo-action="advance">
                    {% block content %}Content
                    {% endblock %}
                </turbo-frame>
            </main>
            <footer>
                <turbo-frame id="footer" src="{{ path('app_default_footer') }}"></turbo-frame>
            </footer>
        </div>
    </body>
</html>
